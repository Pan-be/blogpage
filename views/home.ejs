<%- include('partials/header') -%>

    <h1>Home</h1>

    <p>
        <%= content %>
    </p>

    <% posts.map((post)=> { %>
        <% console.log(post.title) %>
            <% }) %>

                <%- include('partials/footer') -%>